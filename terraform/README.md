# Manytask Infrastructure - Terraform Configuration

Автоматическое развертывание инфраструктуры Manytask в Yandex Cloud с использованием Terraform и cloud-init.

## 🎯 Обзор

Эта конфигурация Terraform автоматически разворачивает полную инфраструктуру для Manytask:

- **GitLab Server** - Self-hosted GitLab CE для управления репозиториями студентов
- **Manytask Server** - Приложение Manytask с nginx-proxy и автоматической настройкой SSL сертификатов
- **PostgreSQL Server** - Отдельный сервер базы данных в приватной подсети (для экономии используется виртуалка с развернутым PostgreSQL, но можно заменить это на managed решение)

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                      Yandex Cloud                           │
│                                                             │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              VPC Network (10.0.0.0/16)                 │ │
│  │                                                        │ │
│  │  ┌────────────────────────────────────────────────┐    │ │
│  │  │     Public Subnet (10.0.1.0/24)                │    │ │
│  │  │                                                │    │ │
│  │  │  ┌──────────────┐  ┌──────────────┐            │    │ │
│  │  │  │   GitLab     │  │  Manytask    │            │    │ │
│  │  │  │   Server     │  │   Server     │            │    │ │
│  │  │  │ 4C/8GB/100GB │  │ 2C/8GB/50GB  │            │    │ │
│  │  │  └──────────────┘  └──────────────┘            │    │ │
│  │  └────────────────────────────────────────────────┘    │ │
│  │                                                        │ │
│  │  ┌────────────────────────────────────────────────┐    │ │
│  │  │    Private Subnet (10.0.2.0/24)                │    │ │
│  │  │                                                │    │ │
│  │  │  ┌──────────────┐                              │    │ │
│  │  │  │ PostgreSQL   │                              │    │ │
│  │  │  │   Server     │                              │    │ │
│  │  │  │ 2C/4GB/50GB  │                              │    │ │
│  │  │  └──────────────┘                              │    │ │
│  │  └────────────────────────────────────────────────┘    │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  DNS Zone: *.yourdomain.com                                 │
└─────────────────────────────────────────────────────────────┘
```

### Компоненты

1. **GitLab Server** (Public)
   - GitLab CE с Docker
   - Автоматическая настройка через cloud-init
   - SSL сертификаты через Let's Encrypt
   - Доступ: `https://gitlab.yourdomain.com`

2. **Manytask Server** (Public)
   - Manytask приложение
   - nginx-proxy для автоматического reverse proxy
   - acme-companion для SSL сертификатов
   - Доступ: `https://manytask.yourdomain.com`

3. **PostgreSQL Server** (Private)
   - PostgreSQL 15

## 📦 Требования

- установить [Terraform](https://developer.hashicorp.com/terraform)
- создать аккаунт в Yandex Cloud
- настроить [Yandex Cloud CLI](https://yandex.cloud/ru/docs/cli/quickstart)
- SSH ключ для доступа к серверам
- Достаточные квоты для ресурсов:
  - 3 виртуальные машины
  - 2 публичных IP адреса
  - ~200GB дискового пространства
- Зарегистрированный домен
- настроить DNS записи домена на сервера ns1.yandexcloud.net. ns2.yandexcloud.net.

## 🚀 Быстрый старт

### 1. Подготовка


```bash
export YC_TOKEN=$(yc iam create-token)
export YC_CLOUD_ID=$(yc config get cloud-id)
export YC_FOLDER_ID=$(yc config get folder-id)
```

```bash
# Перейдите в директорию terraform
cd terraform

# Скопируйте пример конфигурации
cp terraform.tfvars.example terraform.tfvars
```

### 2. Настройка переменных

Отредактируйте `terraform.tfvars`:

```hcl
# Обязательные параметры
yandex_cloud_id  = "b1g..."            # Ваш Cloud ID
yandex_folder_id = "b1g..."            # Ваш Folder ID
domain           = "example.com"       # Ваш домен
ssh_public_key   = "ssh-rsa AAAAB3..." # Ваш SSH публичный ключ
manytask_version = "25.2.0"            # Версия docker образа из Docker Hub https://hub.docker.com/r/manytask/manytask/tags
```

### 3. Инициализация Terraform

```bash
terraform init
```

### 4. Проверка того, что будет добавлено

```bash
terraform plan
```

### 5. Развертывание

```bash
terraform apply
```

Подтвердите развертывание, введя `yes`.

⏱️ **Время развертывания:** ~15-20 минут

### 6. Получение информации
При завершении вся информация будет выведена в консоль, но также можно использовать эти команды
```bash
# Показать все выходные данные
terraform output

# Показать конкретные значения
terraform output gitlab_root_password
terraform output postgresql_password
terraform output deployment_summary
```


### Обновление инфраструктуры

```bash
# Обновить конфигурацию
terraform plan
terraform apply

# Обновить только Manytask
terraform apply -target=yandex_compute_instance.manytask
```

## 📝 После развертывания

### 1. Дождитесь инициализации серверов

GitLab требует 5-10 минут для полной инициализации:

```bash
# Проверить статус GitLab
ssh ubuntu@<gitlab_ip> "docker-compose -f /opt/gitlab/docker-compose.yml logs -f"
```

### 2. Настройте GitLab OAuth

1. Войдите в GitLab как root:
   ```bash
   terraform output gitlab_root_password
   ```

2. Создайте OAuth приложение:
   - Перейдите в Admin Area → Applications
   - Name: `Manytask`
   - Redirect URI: `https://app.yourdomain.com/auth/callback`
   - Scopes: `api`, `sudo`, `profile`, `openid`
   - Сохраните `Client ID` и `Client Secret`

3. Создайте Admin Token:
   - Перейдите в Admin Area → Users → root
   - Access Tokens → Add new token
   - Scopes: `api`, `sudo`
   - Сохраните токен

### 3. Обновите конфигурацию Manytask

```bash
# Подключитесь к Manytask серверу
ssh ubuntu@<manytask_ip>

# Отредактируйте .env файл
sudo nano /opt/manytask/.env

# Обновите следующие переменные:
GITLAB_ADMIN_TOKEN=<your_admin_token>
GITLAB_CLIENT_ID=<your_client_id>
GITLAB_CLIENT_SECRET=<your_client_secret>

# Перезапустите Manytask
cd /opt/manytask
sudo docker-compose restart manytask
```

### Просмотр логов

```bash
# GitLab
ssh ubuntu@<gitlab_ip>
sudo docker-compose -f /opt/gitlab/docker-compose.yml logs -f

# Manytask
ssh ubuntu@<manytask_ip>
sudo docker-compose -f /opt/manytask/docker-compose.yml logs -f

# PostgreSQL
ssh ubuntu@<postgresql_ip>
sudo journalctl -u postgresql -f

# Также на любой виртуалке можно посмотреть логи cloud-init
sudo cat /var/log/cloud-init.log
sudo cat /var/log/cloud-init-output.log
```

## Удаление
При необходимости удалить все что было создано, можно использовать эту команду
```bash
terraform destroy
```
