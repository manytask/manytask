// Reference solution (gold). NOT exported — students receive add.rs.template
// renamed to add.rs. The test drivers pull this in via `include!("add.rs")`.
pub fn add(a: i64, b: i64) -> i64 {
    a + b
}
