name: 'Auto Assign Reviewers'

on:
  pull_request:
    types: [opened, ready_for_review, reopened]

jobs:
  add-reviewers:
    runs-on: ubuntu-latest
    steps:
      - uses: kentaro-m/auto-assign-action@v0.2.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}