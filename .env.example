# =============================================================================
# FLASK CONFIGURATION
# =============================================================================
# Random secret string for Flask sessions
FLASK_SECRET_KEY=random

# =============================================================================
# GITLAB CONFIGURATION
# =============================================================================
GITLAB_URL=https://gitlab.manytask2.org

# -----------------------------------------------------------------------------
# Step 1: Create GitLab Admin Token
# -----------------------------------------------------------------------------
# Go to: GitLab -> Preferences -> Access Tokens
# Create a token with all scopes
GITLAB_ADMIN_TOKEN=

# -----------------------------------------------------------------------------
# Step 2: Create GitLab OAuth Application
# -----------------------------------------------------------------------------
# Go to: GitLab -> Admin Area -> Applications
# Create a new application with the following settings:
#
# Name: manytask (or any name you prefer)
# Redirect URL: http://localhost:8081/login_finish (change 'localhost:8081' to your url if needed)
# Trusted: Yes
# Confidential: Yes
# Scopes (select all):
#   - api (Access the API on your behalf)
#   - read_user (Read your personal information)
#   - read_repository (Allows read-only access to the repository)
#   - sudo (Perform API actions as any user in the system)
#   - openid (Authenticate using OpenID Connect)
#   - profile (Allows read-only access to the user's personal information using OpenID Connect)
#   - email (Allows read-only access to the user's primary email address using OpenID Connect)
#
# After creating the application, copy the credentials below:

# Application ID (from the created OAuth application)
GITLAB_CLIENT_ID=

# Secret (from the created OAuth application)
GITLAB_CLIENT_SECRET=

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Automatically apply all migrations on startup
# The same as create required database tables if they don't exist
APPLY_MIGRATIONS=true

# Username of the initial instance admin (your GitLab username)
INITIAL_INSTANCE_ADMIN=username

# PostgreSQL connection URL in format: postgresql://user:password@host:port/database
# This is used inside Docker containers
DATABASE_URL=postgresql://adminmanytask:adminpass@postgres:5432/manytask

# Same format as DATABASE_URL, used for development in alembic commands
# Change this if you are using a database in a docker container
# For example - postgresql://postgres_user:postgres_password@localhost:5432/postgres_db
DATABASE_URL_EXTERNAL=postgresql://adminmanytask:adminpass@localhost:5432/manytask

# =============================================================================
# DOCKER CONFIGURATION (optional)
# =============================================================================
DOCS_HOST=docs.localhost
APP_HOST=localhost